<div class="row">
	<%= form_for form_params(@topic,@comment) do |form|%>
		<div class="col-lg-6">
			<div class="col-lg-12">
				<%= form.text_area :body,class:"form-control",placeholder:"Yorumunuz",style:"max-height:100px;min-height:100px;"%>
			</div>
			<div class="col-lg-12">
				<%= form.submit "Gönder",class:"btn btn-primary"%>
			</div>
		</div>
		<%= render :partial => "layouts/form_errors",:locals => {object:@comment}%>
	<%end%>
</div>